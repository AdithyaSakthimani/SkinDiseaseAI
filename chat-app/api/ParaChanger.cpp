#include <bits/stdc++.h>
string arrangeStr (string str){
    vector<string> lines ;
    string currLine = "" ; 
    for(int i = 0 ; i<str.size() ; i++){
        if(str[i] == '1' && str[i+1] == '.'){
                lines.push_back
        }
    }
}